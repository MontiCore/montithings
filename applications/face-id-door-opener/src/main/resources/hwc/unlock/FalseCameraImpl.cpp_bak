// (c) https://github.com/MontiCore/monticore
#include "CameraImpl.h"
#include ""
#include <iostream>

namespace montithings {
namespace unlock {

CameraResult
CameraImpl::getInitialValues ()
{
  return {};
}

CameraResult
CameraImpl::compute (CameraInput input)
{
  if (input.getValue ())
    {
      std::cout << input.getValue ().value () << std::endl;
    }
  else
    {
      std::cout << "No data." << std::endl;
    }

  return {};
}

}}