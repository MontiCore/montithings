package hierarchy;

config Sink for GENERIC {
  actuator {
        sendToExternal  = "CustomNoData.ftl";
      }
}

config Sink for DSA {
  actuator {
        sendToExternal  = "invalid.ftl";
      }
}